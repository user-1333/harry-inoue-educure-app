<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.educure.attendancemanagement.mapper.RolePermissionMapper">
    <insert id="insert" parameterType="RolePermission">
        INSERT INTO role_permission (role_id, permission_id) VALUES (#{roleId}, #{permissionId})
    </insert>
    <delete id="delete" parameterType="RolePermission">
        DELETE FROM role_permission WHERE role_id = #{roleId} AND permission_id = #{permissionId}
    </delete>
</mapper>